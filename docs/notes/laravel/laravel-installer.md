# laravel installer

```
composer global require laravel/installer
laravel: command not found

composer global config bin-dir --absolute

sudo nano ~/.bashrc
export PATH="$HOME/.config/composer/vendor/bin:$PATH"

source ~/.bashrc
```

```
sudo -u www-data php artisan app:test
```
